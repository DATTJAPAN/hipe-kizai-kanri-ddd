export type IdAndPrefixedId = {
    id?: number;
    prefixed_id: string;
};

export type ParentUnitId = {
    parent_unit_id: number | null;
};

export type HeadOrgId = {
    head_org_id: number | null;
};

export type OrgId = {
    org_id: number | null;
};

export type CreatorOrgUserId = {
    creator_org_user_id: number | null;
};
